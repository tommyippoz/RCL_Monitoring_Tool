/**
 * 
 */
package ippoz.madness.lite.experiment;

/**
 * @author Tommy
 *
 */
public enum OutputType {
	SINGLE_FILE, 
	EACH_EXPERIMENT, 
	EACH_INDICATOR, 
	EACH_EXPERIMENT_EACH_INDICATOR
}
